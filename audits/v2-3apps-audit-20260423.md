# moon-billing-kmp v2 — 3앱 영향도 감사

> **Date**: 2026-04-23
> **Auditor**: CC via developer_1
> **Parent**: SPLITLY-DAY6-RESULT-20260422 §7 Post-Wave P.3
> **Type**: Read-only audit (수정/빌드/테스트 0건)

---

## 1. Summary

| 앱 | Case | 현재 버전 | 예상 공수 | 주요 변경 |
|---|---|---|---|---|
| HabitFlow | **α** | unspecified (includeBuild, NoOp 단계) | **2h** | dependency re-export + NoOp→실 엔진 치환 대기 |
| CoupleSync | **α** | **v1.0.0** (includeBuild, wrapper 추상화) | **2h** | BillingEngine 인터페이스 정렬 + v2 번들 업데이트 |
| FocusOn | **α** | not_pinned (includeBuild, wrapper 추상화) | **2h** | 모듈 re-export 업데이트만 필요 |

**전체 3앱 모두 Case α**. Splitly가 Day 6에서 겪은 Swift bridge 확장(β) / async 재작성(γ) 이슈는 3앱에 **존재하지 않음**.

**총 예상 공수**: **~6h** (3앱 합계). v1.0.6 출시 직후 wave 단일 실행으로 완료 가능.

---

## 2. 권장 마이그레이션 순서

### 우선순위 1: Splitly v1.0.6 출시 직후 (+0주)
- **모두 Case α** → 모두 1차 wave에 포함 가능
- 권장 실행 순서 (복잡도 오름차순):
  1. **FocusOn** — PurchaseResult callsite 0건, wrapper만 있음. 가장 빠름 (~1.5h)
  2. **CoupleSync** — PurchaseResult callsite 0건, v1.0.0 고정. 버전 bump 중심 (~2h)
  3. **HabitFlow** — PurchaseResult 3-param callsite 2건 존재 (HabitFlowBilling.kt:28, 31). 소급 확인 필요 (~2.5h)

### 우선순위 2: Case β 앱 (해당 없음)
### 우선순위 3: Case γ 앱 (해당 없음)

---

## 3. 3앱 상세

### 3-1. HabitFlow — Case α (2h)

| 항목 | 값 |
|---|---|
| Billing 의존성 | uses_moon_billing_kmp=true, version=unspecified, ref_style=**includeBuild** |
| PurchaseResult callsites | **2건** (HabitFlowBilling.kt:28, 31 — 모두 3-param destructuring) |
| iOS Swift bridge | **존재 안 함** |
| Android BillingClient | **존재 안 함** |
| Receipt 처리 | 없음 (has_receipt_logic=false, has_verify_api_call=false) |
| Fake/NoOp | **`NoOpBillingEngine`** (Android + iOS 양쪽 모두) |
| ViewModel 수 | 30 (viewModelScope.launch 30건) |

**Rationale**: Billing 미구현 단계. 양 플랫폼 모두 NoOp engine 사용 중이며 실 billing 로직 부재. 3-param 은 v2 signature 확장(5-param)에서도 그대로 컴파일 통과.

**Migration blockers**: 없음.

**실 공수 분해**:
- dependency bump: 0.5h
- HabitFlowBilling.kt:28,31 3-param → 5-param wildcard 검증: 1h
- NoOpBillingEngine의 v2 인터페이스 준수 확인: 0.5h

---

### 3-2. CoupleSync — Case α (2h)

| 항목 | 값 |
|---|---|
| Billing 의존성 | uses_moon_billing_kmp=true, version=**v1.0.0**, ref_style=**includeBuild** |
| PurchaseResult callsites | **0건** (wrapper 아래로 완전 추상화) |
| iOS Swift bridge | **존재 안 함** (모든 iOS billing은 Kotlin expect() 경유) |
| Android BillingClient | **존재 안 함** (Firebase 경유) |
| Receipt 처리 | 없음 |
| Fake/NoOp | 없음 |
| ViewModel 수 | 61 (viewModelScope.launch 61건) |

**Rationale**: `couplesync-billing` 전용 submodule이 `BillingEngine` 인터페이스로 moon-billing-kmp를 완전히 감쌈. `createBillingEngine`/`StoreKitBillingEngine` (moon-firebase-billing-kmp 경유) 사용. 앱 레벨에서는 `PurchaseResult` 노출 0건 → v2 5-param 확장에도 영향 없음.

**Migration blockers**: 없음.

**실 공수 분해**:
- v1.0.0 → v2.0.0 bump: 0.5h
- `couplesync-billing` 내부 BillingEngine 인터페이스 정렬: 1h
- Firebase billing wrapper 동작 확인: 0.5h

---

### 3-3. FocusOn — Case α (2h)

| 항목 | 값 |
|---|---|
| Billing 의존성 | uses_moon_billing_kmp=true, version=not_pinned_in_libs.versions.toml, ref_style=**includeBuild** |
| PurchaseResult callsites | **0건** (BillingRepositoryImpl 아래 추상화) |
| iOS Swift bridge | **존재 안 함** |
| Android BillingClient | **존재 안 함** |
| Receipt 처리 | 없음 |
| Fake/NoOp | 없음 |
| ViewModel 수 | 63 (viewModelScope.launch 63건) |

**Rationale**: `focuson-billing` 모듈 + `BillingRepositoryImpl` 이 platform-specific `BillingEngine` (GooglePlayBillingEngine / StoreKitBillingEngine) 에 위임. 앱 레벨에 PurchaseResult / BillingClient / StoreKit 직접 참조 0건.

**Migration blockers**: 없음.

**실 공수 분해**:
- libs.versions.toml 에 v2 명시적 pin 추가: 0.5h
- focuson-billing 모듈 re-export 업데이트: 1h
- 컴파일 검증: 0.5h

---

## 4. Open Questions

1. **IAP 심사 상태** (iap_store_status: `manual_check_needed` for all 3):
   - ASC (App Store Connect): HabitFlow / CoupleSync / FocusOn 각각 IAP 심사 통과 여부 수동 확인 필요
   - Play Console: 동일 — IAP 상품 등록 및 심사 상태 확인
   - Splitly v1.0.6 출시 시점에 이 상태 함께 확인 권장

2. **L2 `moon-app-libs` BaseViewModel 이관 타이밍**:
   - HabitFlow 30 VM / CoupleSync 61 VM / FocusOn 63 VM = 총 154 VM
   - Splitly Day 7 BaseViewModel 산출물 완료 후 재평가
   - 이관 시점: Splitly BaseViewModel이 splitly/에서 실전 검증 2주 이상 경과 후

3. **HabitFlow NoOp → 실 BillingEngine 전환**:
   - 현재 placeholder 단계 → store submission 시점에 실 엔진 필요
   - v2 마이그레이션과 동시 진행 vs 별도 wave 결정 필요

4. **병렬 마이그레이션 가능성**:
   - 3앱 모두 Case α 이므로 worktree 기반 병렬 에이전트 3개 동시 실행 가능
   - 단, `moon-kmp-libs` includeBuild 공유 → 동일 버전으로 일괄 bump 권장

---

## 5. 결론

**3앱 모두 Case α — Splitly Day 6에서 겪은 복잡도 이슈 재현 없음.**

**권장**:
- Splitly v1.0.6 출시 후 1주 이내에 3앱 일괄 마이그레이션
- 총 공수 6h → 1일 내 완료 가능
- FocusOn → CoupleSync → HabitFlow 순서
- 각 앱 `docs/billing-v2-audit.json` 은 migration PR 설명에 레퍼런스로 링크

**"3앱 v2 감사는 미래의 나에게 주는 선물 — Read-only 로 빠르게, 판단은 Splitly 출시 후."**
